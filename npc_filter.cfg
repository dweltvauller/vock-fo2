# V.O.C.K. — NPC include file
#
# List NPC prefixes here (one per line) to process only those characters.
# If this file is absent or empty, all characters are processed.
#
# The prefix is the audio tag stem — the part before the number.
# Supports digit-ending prefixes (e.g. ahs7, rb15) correctly.
#
# Example:
#   mor     # Morlis
#   ahs7    # AHS-7
#   zaius   # Zaius

annvv