# V.O.C.K. Fallout 2 — RPU-shaped source tree lives in data/ (committed).
# wav/ and textgrid/ stay top-level as rebuild metadata.

# Build scratch / generated — do not commit
audio/          # raw voice-actor takes (various formats)
pending/
dat/            # built vock.dat / vock_floats.dat / vock_combat.dat
scripts/        # compiled .INT scratch; the shipped copies live in data/scripts/

# Pre-data-layout category folders (contents now under data/) — ignore stragglers
msg/
acm/
lip/
txt/
loc/

# Editor / worktrees
.claude/

# Python
__pycache__/
*.py[cod]
*.pyo

# macOS
.DS_Store
